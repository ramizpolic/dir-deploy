apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: "dir-dev-appset"
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true
  generators:
    ## Use merge generator to merge the configurations from different sources into a single object
    - merge:
        mergeKeys:
          - values.selector
        generators:
          ## Application configs
          - list:
              elements:
                - appName: dir
                  namespace: dir-dev
                - appName: spire
                  namespace: spire
                - appName: spire-crds
                  namespace: spire-crds
          ## Application Charts
          - git:
              repoURL: "https://github.com/ramizpolic/dir-deploy"
              revision: main
              files:
                - path: "applications/{{ appName }}/dev/**/config.json"
              values:
                selector: "{{ path.basename }}"
          ## Cluster Configurations
          - git:
              repoURL: "https://github.com/ramizpolic/dir-deploy"
              revision: main
              files:
                - path: "clusters/dir/dev/**/cluster-config.json"
              values:
                selector: "{{ path.basename }}"
  template:
    metadata:
      name: "{{ values.selector }}-{{ appName }}-dev-argoapp"
      labels:
        cloud: "{{ values.selector }}"
        env: "dev"
        app: "{{ appName }}"
    spec:
      project: "dir-dev"
      sources:
        - repoURL: "{{chart_repo}}"
          chart: "{{chart_name}}"
          targetRevision: "{{ chart_version }}"
          helm:
            ignoreMissingValueFiles: true
            valueFiles:
              - "$values/applications/{{ appName }}/dev/{{ values.selector }}/values.yaml"
        - repoURL: "https://github.com/ramizpolic/dir-deploy"
          targetRevision: main
          ref: values
      destination:
        server: "{{ cluster_address }}"
        namespace: "{{ namespace }}"
      syncPolicy:
        automated:
          prune: false
        syncOptions:
          - CreateNamespace=true
